    <section class="breadcrumb-option">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb__text">
                        <h4>Add address</h4>
                        <div class="breadcrumb__links">
                            <a href="./index.html">Home</a>
                            <a href="./shop.html">Shop</a>
                            <span>Check Out</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>




<section class="pl-md-5 mt-5 mb-5">
    <div class="col-lg-6 col-md-6">
    <form  id="addressForm">
        <div class="row">
            <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Name<span>*</span></p>
                    <input type="text" name="name" value="{{editAddress.name}}">
                </div>
            </div>
        </div>
        <div class="row">
             <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Address<span>*</span></p>
                    <input type="text" placeholder="Street Address" class="checkout__input__add" name="address" value="{{editAddress.address}}">
        
                </div>
            </div>
        
            <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Town/City<span>*</span></p>
                    <input type="text" name="city" value="{{editAddress.city}}">
                </div>
            </div>
        
            <div class="col-lg-6">
                <div class="checkout__input">
                    <p>State<span>*</span></p>
                    <input type="text" value="kerala" readonly name="state">
                </div>
            </div>

            <input type="text" name="userId" value="{{editAddress.userId}}" hidden>
            
            <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Postcode / ZIP<span>*</span></p>
                    <input type="text" name="pincode" value="{{editAddress.pincode}}">
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Phone<span>*</span></p>
                    <input type="text" name="mobile" value="{{editAddress.mobile}}">
                </div>
            </div>

            <input type="text" name="addressId" value="{{editAddress._id}}" hidden>
            {{!-- <div class="col-lg-6">
                <div class="checkout__input">
                    <p>Email<span>*</span></p>
                    <input type="text" name="email" value="{{editAddress.email}}">
                </div>
            </div> --}}
        </div>
            <button type="submit" class="site-btn">submit</address></button>
    </form>
</div>
</section>

<script>
    $("#addressForm").submit((e)=>{
        e.preventDefault()
        $.ajax({
            url:'/address/edit',
            method:'patch',
            data:$("#addressForm").serialize(),
            success:(response)=>{
                if(response.udpatedStatus){
                    console.log('call is coming here')
                    console.log(response)
                    window.location.href='/address-update'
                }
            }
        })
    })
</script>
